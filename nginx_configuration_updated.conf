proxy_cache_path /www/wwwroot/mercury.vm.kumpeapps.com/proxy_cache_dir levels=1:2 keys_zone=mercury_vm_kumpeapps_com_cache:20m inactive=1d max_size=5g;
server {
    listen 80;
    listen 443 ssl http2;
    listen [::]:80;
    server_name mercury.vm.kumpeapps.com mercury.vm.kumpes.com mercury.kumpes.com;
    index index.php index.html index.htm default.php default.htm default.html;
    root /www/wwwroot/mercury.vm.kumpeapps.com;

    #CERT-APPLY-CHECK--START
    # Configuration related to file verification for SSL certificate application - Do not delete
    include /www/server/panel/vhost/nginx/well-known/mercury.vm.kumpeapps.com.conf;
    #CERT-APPLY-CHECK--END

    #SSL-START SSL related configuration, do NOT delete or modify the next line of commented-out 404 rules
    #error_page 404/404.html;
    ssl_certificate    /www/server/panel/vhost/cert/mercury.vm.kumpeapps.com/fullchain.pem;
    ssl_certificate_key    /www/server/panel/vhost/cert/mercury.vm.kumpeapps.com/privkey.pem;
    ssl_protocols TLSv1.1 TLSv1.2 TLSv1.3;
    ssl_ciphers EECDH+CHACHA20:EECDH+CHACHA20-draft:EECDH+AES128:RSA+AES128:EECDH+AES256:RSA+AES256:EECDH+3DES:RSA+3DES:!MD5;
    ssl_prefer_server_ciphers on;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    add_header Strict-Transport-Security "max-age=31536000";
    error_page 497  https://$host$request_uri;
    #SSL-END
    #REDIRECT START
    
    #REDIRECT END

    #ERROR-PAGE-START  Error page configuration, allowed to be commented, deleted or modified
    #error_page 404 /404.html;
    #error_page 502 /502.html;
    #ERROR-PAGE-END

    #PHP-INFO-START  PHP reference configuration, can be annotated or modified
    
    include enable-php-00.conf;
    #PHP-INFO-END

    #IP-RESTRICT-START Restrict access to IP configuration, IP blacklist and whitelist
    
    #IP-RESTRICT-END

    #BASICAUTH START
    
    #BASICAUTH END

    #SUB_FILTER START
    
    #SUB_FILTER END

    #GZIP START
    
    #GZIP END

    #GLOBAL-CACHE START
    
    #GLOBAL-CACHE END

    #WEBSOCKET-SUPPORT START
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    #WEBSOCKET-SUPPORT END

    #PROXY-CONF-START
    location ^~ / {
      # Flask ProxyFix required headers - all required for proper reverse proxy
      proxy_pass http://172.16.18.18:5001;
      
      # Host header (original host that was used to make the request)
      proxy_set_header Host $host;
      
      # IP address headers - critical for security and logging
      proxy_set_header X-Real-IP $remote_addr;
      proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
      
      # Protocol/scheme headers (http/https) - critical for redirects
      proxy_set_header X-Forwarded-Proto $scheme;
      
      # Host and port information - needed for URL generation
      proxy_set_header X-Forwarded-Host $host;
      proxy_set_header X-Forwarded-Port $server_port;
      proxy_set_header X-Forwarded-Prefix /;
      
      # Session cookie settings for proper handling
      # These ensure cookies work correctly through the proxy
      proxy_cookie_path / /;
      proxy_cookie_domain mercury.vm.kumpeapps.com $host;
      
      # Pass the original request method - important for REST APIs
      proxy_method $request_method;
      
      # Preserve incoming request headers
      proxy_pass_request_headers on;
      
      # Timeouts
      proxy_connect_timeout 60s;
      proxy_send_timeout 600s;
      proxy_read_timeout 600s;
      
      # WebSocket support
      proxy_http_version 1.1;
      proxy_set_header Upgrade $http_upgrade;
      proxy_set_header Connection "upgrade";
      
      # Buffer settings for better performance
      proxy_buffers 8 16k;
      proxy_buffer_size 32k;
    }
    #PROXY-CONF-END

    #SERVER-BLOCK START
    
    #SERVER-BLOCK END

    #Prohibited access to files or directories
    location ~ ^/(\.user.ini|\.htaccess|\.git|\.env|\.svn|\.project|LICENSE|README.md)
    {
        return 404;
    }

    #One click application for SSL certificate verification directory related settings
    location /.well-known{
        allow all;
        root /www/wwwroot/mercury.vm.kumpeapps.com;
    }

    #Prohibit placing sensitive files in the certificate verification directory
    if ( $uri ~ "^/\.well-known/.*\.(php|jsp|py|js|css|lua|ts|go|zip|tar\.gz|rar|7z|sql|bak)$" ) {
        return 403;
    }

    #LOG START
    access_log  /www/wwwlogs/mercury.vm.kumpeapps.com.log;
    error_log  /www/wwwlogs/mercury.vm.kumpeapps.com.error.log;
    #Monitor-Config-Start monitor log sending configuration
    access_log syslog:server=unix:/tmp/bt-monitor.sock,nohostname,tag=47__access monitor;
    error_log syslog:server=unix:/tmp/bt-monitor.sock,nohostname,tag=47__error;
    #Monitor-Config-End
    #LOG END
}
