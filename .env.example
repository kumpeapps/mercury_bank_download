# Database Configuration
DATABASE_URL=mysql+pymysql://username:password@localhost:3306/mercury_bank

# Mercury Bank API Configuration
# Note: API keys are now managed through the web interface, not environment variables
MERCURY_API_URL=https://api.mercury.com
MERCURY_SANDBOX_MODE=true

# Sync Service Configuration
SYNC_DAYS_BACK=30
SYNC_INTERVAL_MINUTES=2
RUN_ONCE=false

# Web Application Configuration
SECRET_KEY=your-secret-key-change-this-in-production
FLASK_ENV=production

# Optional: External Database (if not using Docker MySQL)
# DATABASE_URL=mysql+pymysql://Bot_mercury:LetmeN2it@172.16.21.10:3306/Bot_mercury

# Optional: MySQL Configuration (for local Docker database)
MYSQL_ROOT_PASSWORD=secure_root_password
MYSQL_PASSWORD=secure_mercury_password
